const text = document.getElementById('text')
// if (window.getSelection) {
//   text.textContent = window.getSelection().toString();
// } 
// else if (document.selection) {
//   text.textContent = document.selection.createRange().text;
// }